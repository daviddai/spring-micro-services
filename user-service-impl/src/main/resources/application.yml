spring:
  application:
    name: user-service-client
server:
  port: 8082
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://192.168.1.2:8761/eureka}
  instance:
    preferIpAddress: true