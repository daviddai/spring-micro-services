spring:
  application:
    name: kanban-service-client
server:
  port: 8083
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://192.168.1.2:8761/eureka}
  instance:
    preferIpAddress: true